---
phase: 05-notifications
plan: 01
type: execute
wave: 1
depends_on:
  - 04-01
files_modified:
  - config/initializers/webpush.rb
  - app/services/notifications/setup_reminders.rb
  - app/services/notifications/send_due.rb
  - app/services/notifications/daily_summary.rb
  - app/jobs/reminder_job.rb
  - app/jobs/daily_summary_job.rb
  - app/models/notification_setting.rb
  - db/migrate/*_create_notification_settings.rb
  - app/javascript/controllers/push_notifications_controller.js
autonomous: true
requirements:
  - NOTIF-01
  - NOTIF-02
  - NOTIF-03
  - NOTIF-04
  - NOTIF-05
---

<objective>
Implementar sistema de notificacoes: push PWA, lembretes Telegram, resumo diario, e quiet hours.

**Output:** Notificacoes funcionais para tarefas proximas do vencimento.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Criar model NotificationSetting e migration</name>
  <files>
    db/migrate/*_create_notification_settings.rb
    app/models/notification_setting.rb
  </files>
  <action>
    Migration:
    ```ruby
    create_table :notification_settings do |t|
      t.string :channel, null: false
      t.integer :reminder_minutes, default: 30
      t.boolean :daily_summary, default: true
      t.string :quiet_start
      t.string :quiet_end
      t.boolean :overdue_alert, default: true
      t.timestamps
    end
    ```

    Model:
    ```ruby
    class NotificationSetting < ApplicationRecord
      validates :channel, presence: true, uniqueness: true
      validates :reminder_minutes, numericality: { in: [5, 120] }
    end
    ```
  </action>
</task>

<task type="auto">
  <name>Task 2: Criar servicos de notificacao</name>
  <files>
    app/services/notifications/setup_reminders.rb
    app/services/notifications/send_due.rb
    app/services/notifications/daily_summary.rb
  </files>
  <action>
    Criar diretorio app/services/notifications/.

    Notifications::SetupReminders: agenda lembretes para tarefas com due_date
    Notifications::SendDue: envia lembrete Telegram quando tarefa esta proxima
    Notifications::DailySummary: envia resumo diario as 8h

    Criar jobs correspondentes em app/jobs/.
  </action>
</task>

<task type="auto">
  <name>Task 3: Configurar Solid Queue recurring jobs</name>
  <files>
    config/environments/production.rb
  </files>
  <action>
    Adicionar jobs recorrentes no Solid Queue:
    - DailySummaryJob: todo dia as 8h (usar cron-like schedule)
    - ReminderJob: roda a cada 5 minutos verificando tarefas proximas

    Usar config.solid_queue.schedule = { ... } no production.rb.
  </action>
</task>

<task type="auto">
  <name>Task 4: Criar Push Notifications Stimulus controller</name>
  <files>
    app/javascript/controllers/push_notifications_controller.js
  </files>
  <action>
    Registrar service worker push subscription no navegador.
    Usar VAPID keys para Web Push Protocol.
  </action>
</task>

</tasks>
